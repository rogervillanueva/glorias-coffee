import { CheckoutView } from "@/components/order/CheckoutView";

export const metadata = {
  title: "Checkout | Gloria's Coffee Bar",
  description: "Review your order before sending it to the baristas.",
};

export default function CheckoutPage() {
  return <CheckoutView />;
}
